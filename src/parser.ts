import { Token } from "./token.ts";

/**
 * Takes Tinytalk tokens and returns an Abstract Syntax Tree
 * @param tokens An array of tokens
 * @returns AST
 */
function parse(tokens: Token[]) {
  return {};
}

export { parse };
